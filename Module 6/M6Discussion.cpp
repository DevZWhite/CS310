#include <iostream>
using namespace std;

enum Color { RED, GREEN, BLUE };

int main() {
    Color myColor = YELLOW;

    if (mycolor == RED) {
        cout << "The color is red." << endl;
    }

    return 0;
}
